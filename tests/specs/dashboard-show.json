[
  {
    "name": "My Habits — empty state",
    "steps": [
      {
        "send": { "callback": "dashboard:show" },
        "expect": [
          { "method": "editMessageText", "payload": { "text": "No habits yet — tap ➕ New Habit to create your first one." } }
        ]
      }
    ]
  }
]
